{
  "name": "twilio-live-interactive-video",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "pushd composer && npm run build && popd && pushd apps/web && npm run build && popd && ln -sf ../composer/build ./assets/composer && ln -sf ../apps/web/build ./assets/app && twilio-run deploy"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/twilio/twilio-live-interactive-video.git"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1",
    "twilio": "^3.59.0",
    "twilio-run": "^2.9.0"
  },
  "bugs": {
    "url": "https://github.com/twilio/twilio-live-interactive-video/issues"
  },
  "homepage": "https://github.com/twilio/twilio-live-interactive-video#readme"
}
